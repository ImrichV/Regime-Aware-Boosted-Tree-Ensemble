# Recovered Research History

The public repository was reconstructed from eight binary chunks containing the complete continuation archive through Module 06.

## Verified private-archive state

- 151 automated tests passed;
- Python compilation passed;
- repository doctor passed;
- eleven candidate families were frozen;
- 561,793 candidate IDs were represented in the accepted outcome catalogue;
- the baseline gate trained 132 fixed regularized models across six walk-forward folds;
- no hyperparameter search was allowed in the baseline gate;
- candidate generators were subjected to prefix-causality and determinism audits.

## Meaning of the baseline gate

The accepted baseline stage used a frozen raw setup score, Ridge regression, and L2 logistic regression. It decided only whether a family showed enough development evidence to justify more flexible specialist research. It did not approve a tradeable system.

## Public/private boundary

The public release preserves generic software contracts and synthetic tests. Private thresholds, generated artifacts, raw data, and model outputs remain outside GitHub.
